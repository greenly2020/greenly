query GetArticlesList($filters: ArticleFiltersInput, $pagination: PaginationArg, $sort: [String]) {
  articles(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      id
      attributes {
        ...Article
      }
    }
    meta {
      ...Pagination
    }
  }
}
